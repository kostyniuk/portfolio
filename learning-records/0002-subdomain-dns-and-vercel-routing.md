# Subdomain DNS and Vercel routing

The user successfully configured `www.lovable.a13x.space` by separating the three layers: DNS CNAME record, Vercel project domain assignment, and Next.js host-based rewrite. This matters for future teaching because DevOps topics should start with layer ownership and concrete failure modes, especially A vs CNAME records, relative DNS names, and HTTPS validation.
