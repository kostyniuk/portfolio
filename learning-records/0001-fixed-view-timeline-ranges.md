# Fixed View Timeline Ranges

The learner correctly identified that a scroll-triggered reveal can use the same fixed attachment range for every item, such as `cover 0% cover 20%`, matching the existing `viewport-reveal` mental model. Future lessons should introduce dynamic per-sibling ranges only after the fixed-range model is comfortable.
