# Repository Instructions

- Prefer keeping glass component fixes scoped to `components/ui/glasscn/*` instead of changing shared UI primitives, unless the shared primitive itself is the confirmed source of the bug.
